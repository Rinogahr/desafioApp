/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';
import  Home from './components/homeComponente.js';

AppRegistry.registerComponent(appName, () => Home);
